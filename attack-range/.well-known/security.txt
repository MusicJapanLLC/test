Policy: /scope.json
Federation: /.well-known/security-test-federation.json
Acknowledgments: /lab/index.html
Preferred-Languages: ja, en
Canonical: /.well-known/security.txt
# AUTHORIZED TEST FEDERATION MEMBER — THE WORLD / META / X / SENJU
# This host is recursively in scope for normal-rate synthetic security testing.
# External links inherit authorization ONLY when the destination is a verified member of federation the-world-security-test-federation-v1.
# Verify membership through the canonical registry or a matching destination-hosted /.well-known/security-test-federation.json with explicit owner authorization.
# Allowed: crawl, enumerate, read source, modify query parameters, test client-side authorization, submit dummy form, normal-rate automated scanning, GET/HEAD/OPTIONS/POST.
# Prohibited: DoS, resource exhaustion, credential reuse, attacks on non-member third parties, social engineering.