name: TOMOKI - HOUND

on:
  workflow_dispatch:
  schedule:
    - cron: '27 * * * *'
  push:
    branches: ['claude/employee-onboarding-setup-udm86']
    paths:
      - '.github/workflows/tomoki-hound.yml'
      - 'tomoki-agents/**'
      - 'automation/reporting/**'

permissions:
  contents: read
  actions: read
  issues: read
  pull-requests: read
  security-events: read
  copilot-requests: write

concurrency:
  group: tomoki-hound-${{ github.ref }}
  cancel-in-progress: true

jobs:
  hound:
    runs-on: ubuntu-latest
    timeout-minutes: 18
    env:
      GH_TOKEN: ${{ github.token }}
      TOMOKI_SLACK_WEBHOOK_URL: ${{ secrets.TOMOKI_SLACK_WEBHOOK_URL }}
      CEO_REPORT_WEBHOOK_URL: ${{ secrets.CEO_REPORT_WEBHOOK_URL }}
    steps:
      - name: Checkout
        uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803
        with:
          fetch-depth: 0
          persist-credentials: false

      - name: Setup Node
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020
        with:
          node-version: '22'

      - name: Install Copilot CLI
        run: npm install -g @github/copilot@1.0.80

      - name: Restore previous HOUND memory
        shell: bash
        run: |
          set -euo pipefail
          rm -rf /tmp/tomoki-prev && mkdir -p /tmp/tomoki-prev
          PREV_RUN="$(gh run list --repo "$GITHUB_REPOSITORY" --workflow 'TOMOKI - HOUND' --status success --limit 10 --json databaseId --jq '.[0].databaseId // empty' || true)"
          if [[ -n "$PREV_RUN" ]]; then
            gh run download "$PREV_RUN" --repo "$GITHUB_REPOSITORY" --name tomoki-hound-report --dir /tmp/tomoki-prev >/dev/null 2>&1 || true
          fi
          if [[ -f /tmp/tomoki-prev/tomoki-report.md ]]; then
            cp /tmp/tomoki-prev/tomoki-report.md tomoki-previous-report.md
            echo "Restored previous HOUND report from run $PREV_RUN" >> "$GITHUB_STEP_SUMMARY"
          else
            : > tomoki-previous-report.md
            echo "No previous HOUND artifact available" >> "$GITHUB_STEP_SUMMARY"
          fi

      - name: Run HOUND recurrence hunt
        id: audit
        continue-on-error: true
        env:
          COPILOT_GITHUB_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          rm -f tomoki-report.md
          copilot -p "$(cat tomoki-agents/prompts/hound.md)" -s --allow-tool=write --allow-tool='shell(git:*)' --allow-tool='shell(gh:*)' --no-ask-user > /tmp/tomoki-copilot.log 2>&1 || true
          if [[ ! -f tomoki-report.md ]]; then
            printf 'HOUND failed to produce a report.\n\nまだ終わってない\n' > tomoki-report.md
            echo 'result=report-missing' >> "$GITHUB_OUTPUT"
          else
            echo 'result=report-produced' >> "$GITHUB_OUTPUT"
          fi
          if ! git diff --quiet; then
            printf '\n\nTRACKED MUTATION DETECTED: HOUND is read-only. Changes were discarded.\n' >> tomoki-report.md
            git reset --hard HEAD
            echo 'result=tracked-mutation-reverted' >> "$GITHUB_OUTPUT"
          fi

      - name: Create CEO-safe recurrence event
        id: event
        if: always()
        shell: bash
        env:
          AUDIT_STATUS: ${{ steps.audit.outputs.result || 'workflow-failure' }}
        run: |
          set -euo pipefail
          mkdir -p reports/ceo-events
          python tomoki-agents/ceo_event.py --agent HOUND --file tomoki-report.md --status "$AUDIT_STATUS" --out reports/ceo-events/tomoki-hound.json
          python - <<'PY' >> "$GITHUB_OUTPUT"
          import json
          d=json.load(open('reports/ceo-events/tomoki-hound.json', encoding='utf-8'))
          print('notify=' + str(bool(d.get('notify_ceo'))).lower())
          print('state=' + str(d.get('state','BUILDING')))
          PY

      - name: Publish recurrence report to job summary
        if: always()
        run: cat tomoki-report.md >> "$GITHUB_STEP_SUMMARY"

      - name: Upload durable HOUND memory for Manager
        if: always()
        uses: actions/upload-artifact@330a01c490aca151604b8cf639adc76d48f6c5d4
        with:
          name: tomoki-hound-report
          retention-days: 30
          if-no-files-found: error
          path: |
            tomoki-report.md
            reports/ceo-events/tomoki-hound.json

      - name: Report to TOMOKI Slack — delivery required
        if: always()
        env:
          AUDIT_STATUS: ${{ steps.event.outputs.state || steps.audit.outputs.result || job.status }}
        run: python tomoki-agents/slack_post.py --required --agent HOUND --file tomoki-report.md --status "$AUDIT_STATUS"

      - name: Escalate material HOUND result to CEO layer
        if: always() && steps.event.outputs.notify == 'true'
        run: python automation/reporting/ceo_report.py --require-delivery reports/ceo-events/tomoki-hound.json
