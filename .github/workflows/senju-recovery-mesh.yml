name: Senju Recovery Mesh

on:
  push:
    paths:
      - 'senju/senju/recovery_mesh.py'
      - 'senju/tests/test_recovery_mesh.py'
      - 'senju/RECOVERY_MESH.md'
      - 'senju/pyproject.toml'
      - '.github/workflows/senju-recovery-mesh.yml'
  pull_request:
    paths:
      - 'senju/senju/recovery_mesh.py'
      - 'senju/tests/test_recovery_mesh.py'
      - 'senju/RECOVERY_MESH.md'
      - 'senju/pyproject.toml'
      - '.github/workflows/senju-recovery-mesh.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  recovery-mesh-tests:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      - name: Setup Python
        uses: actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065 # v5
        with:
          python-version: '3.12'

      - name: Install Senju and test dependency
        run: python -m pip install --disable-pip-version-check -e './senju[dev]'

      - name: Run Recovery Mesh tests
        run: pytest -q senju/tests/test_recovery_mesh.py

      - name: Compile Recovery Mesh module
        run: python -m py_compile senju/senju/recovery_mesh.py
