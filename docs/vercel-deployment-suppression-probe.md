# Vercel deployment suppression probe

Purpose: verify that non-default Git branches no longer create Vercel deployments after the allow-list change in PR #602.

This branch is intentionally non-production and contains no runtime or deployment-permit change.
