const root=document.documentElement;const body=document.body;const buttons=[...document.querySelectorAll('[data-lang-btn]')];const searchPanel=document.querySelector('#searchPanel');const searchInput=document.querySelector('#searchInput');const searchResults=document.querySelector('#searchResults');
function currentLang(){return body.dataset.lang||'ja'}
buttons.forEach(b=>b.addEventListener('click',()=>{const target=b.dataset.langBtn;const now=currentLang();localStorage.setItem('djn-lang',target);if(target===now)return;const next=location.pathname.replace(/^\/(ja|en)(?=\/)/,`/${target}`);location.assign(next===location.pathname?`/${target}/`:next)}));
function openSearch(){if(!searchPanel)return;searchPanel.classList.add('open');searchPanel.setAttribute('aria-hidden','false');setTimeout(()=>searchInput?.focus(),50)}function closeSearch(){if(!searchPanel)return;searchPanel.classList.remove('open');searchPanel.setAttribute('aria-hidden','true')}
document.querySelectorAll('[data-open-search]').forEach(b=>b.addEventListener('click',openSearch));document.querySelectorAll('[data-close-search]').forEach(b=>b.addEventListener('click',closeSearch));searchPanel?.addEventListener('click',e=>{if(e.target===searchPanel)closeSearch()});document.addEventListener('keydown',e=>{if(e.key==='Escape')closeSearch();if((e.metaKey||e.ctrlKey)&&e.key.toLowerCase()==='k'){e.preventDefault();openSearch()}});
if(searchInput){const articles=JSON.parse(document.querySelector('#search-data')?.textContent||'[]');searchInput.addEventListener('input',()=>{const q=searchInput.value.trim().toLowerCase();const lang=body.dataset.lang||'ja';if(!q){searchResults.innerHTML='';return}const rows=articles.filter(a=>[a.titleJa,a.titleEn,a.categoryJa,a.categoryEn,a.slug].join(' ').toLowerCase().includes(q)).slice(0,8);searchResults.innerHTML=rows.length?rows.map(a=>`<a class="search-result" href="/${lang}/articles/${a.slug}/"><small>${lang==='ja'?a.categoryJa:a.categoryEn}</small><strong>${lang==='ja'?a.titleJa:a.titleEn}</strong></a>`).join(''):`<div class="empty-state">${lang==='ja'?'該当する記事がありません':'No matching stories'}</div>`})}
document.querySelectorAll('[data-category]').forEach(chip=>chip.addEventListener('click',()=>{document.querySelectorAll('[data-category]').forEach(c=>c.classList.remove('active'));chip.classList.add('active');const cat=chip.dataset.category;document.querySelectorAll('[data-story-category]').forEach(card=>{card.style.display=(cat==='All'||card.dataset.storyCategory===cat)?'':'none'})}));document.querySelector('[data-back-top]')?.addEventListener('click',()=>window.scrollTo({top:0,behavior:'smooth'}));
